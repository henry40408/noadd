# main.rs only wires a live process together and is not unit-testable.
ignore:
  - "src/main.rs"

coverage:
  status:
    project:
      default:
        # Fail only when a PR drops coverage by more than the threshold.
        target: auto
        threshold: 1%
    patch:
      default:
        target: 80%
